{
  "name": "parametric-svg",
  "version": "0.0.1",
  "description": "- generate shapes programmatically - less manual work in prepping files for cutting - parametric (i.e. for material thickness)",
  "main": "index.js",
  "scripts": {
    "build": "node palm-slices.js",
    "watch": "chokidar \"*.js\" -c \"npm run build\"",
    "prettier": "prettier *.js --write",
    "lint": "eslint *.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "John Teske",
  "license": "MIT",
  "dependencies": {
    "chokidar-cli": "^2.0.0",
    "eslint": "^6.5.1",
    "paper-jsdom": "^0.12.3",
    "prettier": "^1.18.2"
  }
}
